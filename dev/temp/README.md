# dev/temp 目录说明

临时工作区，可清理，但清理前要确认没有重要产物。

| 目录 | 用途 |
|------|------|
| `scratch/` | 临时试算、随手草稿 |
| `drafts/` | 临时文案、半成品草稿 |
| `test-runs/` | 临时测试输出、一次性运行结果 |

清理提示：

- `scratch/` 可以清理。
- `test-runs/` 可以清理，但正式测试结论要先写入 `dev/docs/测试记录.md`。
- `drafts/` 清理前确认是否已经转移到 `outputs/drafts/` 或正式文档。
